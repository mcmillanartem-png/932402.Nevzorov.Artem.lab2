# Лабораторная №2: CSS

## Файлы
- `index.html` — разметка
- `style.css` — стили

## Реализация
- Внешний CSS + 1 inline (`body`) + 1 правило в `<style>`
- Переменные в `:root`
- Позиционирование: static, relative, absolute
- Псевдоклассы: :hover, :active, :focus, :visited
- Контраст ≥ 4.5:1